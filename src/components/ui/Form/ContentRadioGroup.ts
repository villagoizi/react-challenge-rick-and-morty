import styled from "@emotion/styled";

export const ContentRadioGroup = styled("div")`
  display: flex;
  padding: 1rem;
  align-items: center;
  label {
    margin-left: 1rem;
    font-size: 1.5rem;
    font-family: var(--font-jock);
    color: var(--terciary);
  }
`;
